
use proconio::input;

fn main() {
    input! {
        n: i32,
    }
    let mut result: f64 = 0.0;
    for i in 0..n {
        if x - (a + 0.5) > 0.0 && (result > p || result == 0.0) {
          // println!("x - ( a + 0.5) = {}", x - ( a + 0.5));
          // println!("a = {}", a);
            result = p;
        }
    }
    println!("{}", result);
}


